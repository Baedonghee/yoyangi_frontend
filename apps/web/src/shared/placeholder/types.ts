export type SampleBlock = {
  label: string;
  title: string;
  description: string;
};

export type PlaceholderConfig = {
  title: string;
  description: string;
  eyebrow: string;
  heroNote: string;
  ctaLabel: string;
  tags: string[];
  blocks: SampleBlock[];
};

